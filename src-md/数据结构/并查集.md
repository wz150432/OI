# 并查集

并查集是一种 $\Theta(1)$ 判断两个元素是否是一个集合并且 $\Theta(1)$ 合并两个集合的数据结构。



两种优化，一种是路径压缩，另一种是按秩合并。

路径压缩就是递归的过程中，直接让当前元素指向这个集合的代表元素或者根节点作为父节点。

```cpp
int find(int x) {
    if (p[x] != x) p[x] = find(p[x]);
    return p[x];
}
```

按秩合并就是当我们合并两个集合的时候我们可以层数较小的内个并入层数较多的内个



出了普通的并查集，我们还有带权并查集。
